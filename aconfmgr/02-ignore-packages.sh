#!/bin/bash

# Ignore packages that are usually installed with the base system
# or get installed as dependencies of other packages.
# Some programs may be also provided by `asdf`

# Ignore yarn
IgnorePackage yarn
